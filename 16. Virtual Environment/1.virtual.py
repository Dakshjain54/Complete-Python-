'''
Virtual Environment:
is a tool used to isolate speciffic python environment on a single 
machine, allowing you to work on multiple projects
Packages that are not compatible with each other.

How importing in python works:
is the process of loading code from a python module into the
current script 
'''

from math import sqrt,pi
result = sqrt(9)* pi
print(result)

# import pyjokes
# print(pyjokes.get_joke())

# import math
# print(dir(math))
            